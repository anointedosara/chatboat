/**
 * Number of digits in the OTP.
 *
 * Twilio Verify issues 6-digit codes by default. If you set your Twilio Verify
 * service to a different length, change this to match (and the UI follows).
 */
export const OTP_LENGTH = 6;
